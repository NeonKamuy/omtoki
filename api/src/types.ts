const TYPES = {
    Models: {
        User: Symbol("User Model"),
        Event: Symbol("Event Model")
    },
    Guards: {
        AdminAuth: Symbol("Admin Auth Guard")
    }
}

export default TYPES;